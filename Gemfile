source "https://rubygems.org"
gemspec

# Ensures the proper Jekyll version is running.
gem "jekyll", "3.5.0"

# Plugins
group :jekyll_plugins do
   gem "jekyll-feed", "~> 0.6"
   gem 'jekyll-archives'
   
end
